apiVersion: v1
kind: Namespace
metadata:
  name: cilium-lbaas
---
# https://kubernetes.io/docs/concepts/workloads/controllers/deployment/
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lb-controller
  namespace: cilium-lbaas
  labels:
    app: lb-controller
spec:
  selector:
    matchLabels:
      app: lb-controller
  replicas: 1
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: lb-controller
    spec:
      serviceAccountName: lb-controller
      tolerations:
        - key: node-role.kubernetes.io/control-plane
          effect: NoSchedule
      volumes:
        - name: stackit-creds
          secret:
            secretName: stackit-cloud-secret
            items:
              - key: serviceaccount.json
                path: serviceaccount.json
            optional: true
      containers:
      - name: lb-controller
        image: ghcr.io/hown3d/cilium-lb/lb-controller
        args:
          - --project-id=$(PROJECT_ID)
          - --network-id=$(NETWORK_ID)
        imagePullPolicy: IfNotPresent
        env:
          - name: STACKIT_SERVICE_ACCOUNT_KEY_PATH
            value: /.stackit/serviceaccount.json
        envFrom:
          - configMapRef:
              name: lb-config
        volumeMounts:
          - mountPath: /.stackit
            name: stackit-creds
---
